# Motor Control via SocketCan

## Installation
```
pip install python-can
```

## Setup
run `scripts/setup_can.sh` firstly to setup CAN connection. You may need modify the `/dev/ttyACM0` based on yout connection.




