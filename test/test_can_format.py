import struct

data = bytes([0x00,0x00,0x40,0xC0, 0x00,0x00,0x80,0x3F]) # 0x00, 0x00, 0x40, 0x40, 0x00, 0x00, 0x80, 0x3F
print(list(data))

pos, vel = struct.unpack("<ff", data)
print(pos, vel)

data_rec = struct.pack("<ff", -3.0, 1.0)
print(list(data_rec))
